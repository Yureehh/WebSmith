# Source: Product Status

Path: `data/raw/websmith/product-docs/PRODUCT_STATUS.md`

## Assertions

- WebSmith `blocks` user accounts (confidence 0.95)
- WebSmith `blocks` auth (confidence 0.95)
- WebSmith `blocks` billing (confidence 0.95)
- WebSmith `blocks` SaaS tenancy (confidence 0.95)
- WebSmith `blocks` public deployment (confidence 0.95)
- WebSmith `blocks` automatic email sending (confidence 0.95)
- WebSmith `blocks` paid map discovery APIs (confidence 0.95)
- WebSmith `blocks` legal advice (confidence 0.95)
- WebSmith `blocks` finalized client contracts (confidence 0.95)
- WebSmith `classifies` Website opportunities (confidence 0.90)
- WebSmith `classifies` market classification (confidence 0.95)
- market classification `classifies` B2C (confidence 0.95)
- market classification `classifies` B2B (confidence 0.95)
- market classification `classifies` B2C+B2B (confidence 0.95)
- market classification `classifies` unknown market (confidence 0.95)
- lead storage `contains` discovered queue (confidence 0.95)
- lead storage `contains` active queue (confidence 0.95)
- lead storage `contains` archive queue (confidence 0.95)
- manual-only CRM actions `contains` draft (confidence 0.95)
- manual-only CRM actions `contains` sent marker (confidence 0.95)
- manual-only CRM actions `contains` received answer (confidence 0.95)
- manual-only CRM actions `contains` follow-up (confidence 0.95)
- manual-only CRM actions `contains` do-not-contact (confidence 0.95)
- manual-only CRM actions `contains` won (confidence 0.95)
- manual-only CRM actions `contains` lost (confidence 0.95)
- manual-only CRM actions `contains` archive (confidence 0.95)
- review extraction `depends_on` available public pages (confidence 0.95)
- WebSmith `discovers` local Italian businesses (confidence 0.95)
- WebSmith `generates` external website workspaces (confidence 0.95)
- OSM/Overpass discovery `generates` coverage metadata (confidence 0.90)
- website workspace generation `generates` BUILD_BRIEF.md (confidence 0.95)
- WebSmith `generates` brief/workspace (confidence 0.95)
- WebSmith `implements` local managed-service cockpit (confidence 0.95)
- WebSmith `manages` Manual outreach (confidence 0.95)
- market classification `reads_from` category data (confidence 0.95)
- website building `related_to` external builder (confidence 0.90)
- OpenAI-backed enrichment `requires` OPENAI_API_KEY (confidence 0.95)
- web-search website lookup `requires` OPENAI_API_KEY (confidence 0.95)
- outreach `requires` OPENAI_API_KEY (confidence 0.90)
- reply drafting `requires` OPENAI_API_KEY (confidence 0.95)
- selected-lead web lookup `requires` OPENAI_API_KEY (confidence 0.95)
- lead storage `runs_on` SQLite (confidence 0.90)
- selected-lead web lookup `runs_on` Enrich (confidence 0.95)
- WebSmith `stores` lead storage (confidence 0.90)
- explicit enrichment action `stores` source document storage (confidence 0.90)
- WebSmith `supports` explicit enrichment action (confidence 0.95)
- WebSmith `supports` OpenAI-backed enrichment (confidence 0.95)
- WebSmith `supports` web-search website lookup (confidence 0.95)
- WebSmith `supports` outreach (confidence 0.90)
- WebSmith `supports` reply drafting (confidence 0.95)
- WebSmith `supports` manual-only CRM actions (confidence 0.95)
- WebSmith `supports` website workspace generation (confidence 0.95)
- BUILD_BRIEF.md `supports` Codex Sites (confidence 0.90)
- BUILD_BRIEF.md `supports` local Codex (confidence 0.90)
- BUILD_BRIEF.md `supports` Claude Code (confidence 0.90)
- BUILD_BRIEF.md `supports` external builder (confidence 0.90)
- WebSmith `uses` OSM/Overpass discovery (confidence 0.95)
- OSM/Overpass discovery `uses` Activity filters (confidence 0.95)
- OSM/Overpass discovery `uses` Negative filters (confidence 0.95)
- OSM/Overpass discovery `uses` Search depth (confidence 0.95)
- OSM/Overpass discovery `uses` Dedupe (confidence 0.95)
