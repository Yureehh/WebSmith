# Source: Claude Code Vscode

Path: `data/raw/websmith/website-factory-docs/CLAUDE_CODE_VSCODE.md`

## Assertions

- Claude Code `blocks` WebSmith repo root (confidence 0.78)
- QA review `blocks` layout issues (confidence 0.82)
- QA review `blocks` weak copy (confidence 0.82)
- QA review `blocks` generic sections (confidence 0.82)
- QA review `blocks` spacing problems (confidence 0.82)
- QA review `blocks` unsupported claims (confidence 0.82)
- VS Code `contains` website-projects/<id>-<slug>/ (confidence 0.72)
- website `depends_on` WebSmith workspace (confidence 0.81)
- WebSmith `enriches` lead (confidence 0.88)
- site/ `exports` exports/ (confidence 0.90)
- Generate website `generates` website-projects/<id>-<slug>/ (confidence 0.86)
- planning prompt `reads_from` BUILD_BRIEF.md (confidence 0.96)
- build prompt `reads_from` BUILD_BRIEF.md (confidence 0.96)
- WebSmith `reads_from` site path (confidence 0.84)
- WebSmith `reads_from` zip path (confidence 0.84)
- WebSmith `reads_from` preview URL (confidence 0.84)
- factual claim traceability `requires` source_documents/ (confidence 0.86)
- factual claim traceability `requires` site/DECISIONS.md (confidence 0.86)
- Claude Code `runs_on` website-projects/<id>-<slug>/ (confidence 0.83)
- python3 -m http.server 4173 -d site `runs_on` site/ (confidence 0.90)
- Claude Code VS Code Workflow `supports` website (confidence 0.92)
- QA review `validates` desktop (confidence 0.90)
- QA review `validates` mobile (confidence 0.90)
